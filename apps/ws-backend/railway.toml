[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm run build --filter=ws-backend..."

[deploy]
startCommand = "node apps/ws-backend/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
