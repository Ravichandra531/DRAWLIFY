[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm run build --filter=http-backend..."

[deploy]
startCommand = "node apps/http-backend/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
